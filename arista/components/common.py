# For backward compatibility - there are a bunch of things
# which import the following things from arista.components.common,
# but they are actually moved to other places.
from ..core.component import PciComponent
